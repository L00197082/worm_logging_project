version: '3.8'

services:
  worm-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Named volume keeps data persistent, simulating an external secure store
      - ./data:/data
    environment:
      - PYTHONUNBUFFERED=1
volumes:
  worm-data: